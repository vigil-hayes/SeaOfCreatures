typedef double Centimeters; // used for an Organism's size

// Possible types for an Organism
enum Type { PLANT, CREATURE };

// Possible colors for an Organism
enum Color { RED, ORANGE, YELLOW, GREEN, BLUE, PURPLE };

// Possible states of life
enum Status { HEALTHY, SICK, DEAD };
